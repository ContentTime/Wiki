{
  "type": "module",
  "devDependencies": {
    "@unocss/preset-attributify": "0.65.3",
    "@unocss/transformer-directives": "^0.65.3",
    "@unocss/transformer-variant-group": "0.65.3",
    "sass": "^1.83.1",
    "unocss": "^0.63.6",
    "vitepress": "^1.5.0",
    "vitepress-plugin-tabs": "^0.5.0",
    "vitepress-sidebar": "^1.30.2",
    "vue": "^3.5.13",
    "markdownlint-cli": "^0.43.0"
  },
  "scripts": {
    "dev": "bun vitepress dev --port 4003",
    "devhost": "bun vitepress dev --host --port 4003",
    "ebuild": "bun vitepress build",
    "preview": "bun vitepress preview --host --port 4003",
    "check": "markdownlint *.md && markdownlint src/",
    "attemptfix": "markdownlint --fix *.md && markdownlint --fix src/ && git status"
  },
  "dependencies": {
    "@vueuse/core": "^12.3.0"
  }
}
